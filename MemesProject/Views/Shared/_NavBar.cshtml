@using System.Activities.Statements

<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Memes", "Index", "Home", new {area = ""}, new {@class = "navbar-brand"})
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li>
                    <a href="@Url.Action("Index", "Home")" class="fa fa-lg fa-home "> Strona główna</a>
                </li>
                <li>
                    <a href="@Url.Action("New", "Meme")" class="fa fa-lg fa-plus "> Dodaj mema</a>
                </li>
                @if (User.Identity.IsAuthenticated)
                {
                    <li>@Html.ActionLink("Twoje konto", "Index", "User")</li>
                }
                <li>
                    <a href="@Url.Action("RandomMeme", "Meme")" class="fa fa-lg fa-random "> Losuj mema</a>
                </li>
                <li>
                    <a href="@Url.Action("About", "Home")" class="fa fa-lg fa-info "> Informacje</a>
                </li>
            </ul>
            @Html.Partial("_LoginPartial")
        </div>
    </div>
</div>